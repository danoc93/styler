import React from "react";
import ReactDOM from "react-dom";
import { App } from "./App";

/**
 * Entry Point for Styler App
 * This application manages video processing requests.
 */

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
