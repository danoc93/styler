/**
 * Processor for adding two numbers.
 * This processor is used by the queue to handle all the task.add jobs.
 * It is used mostly to test the job queue is able to operate correctly.
 */
module.exports = (a, b) => a + b;
